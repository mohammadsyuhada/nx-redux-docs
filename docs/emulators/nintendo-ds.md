# Nintendo DS

Nintendo DS games run on a bundled **Drastic** emulator. Put your ROMs in
`Roms/Nintendo DS (NDS)/`.

## Screen layout and theme

- Hold `SELECT` + `Left` / `Right` to cycle the screen layout (how the two DS
  screens are arranged on the device's single screen).
- Hold `SELECT` + `Y` to cycle the theme.
